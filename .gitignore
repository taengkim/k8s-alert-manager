# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/
*.tsbuildinfo

# SQLite
*.db
*.sqlite3
# WAL-mode side files (app.db now enables journal_mode=WAL for SQLite)
*.db-shm
*.db-wal

# OMC / superpowers
.omc/
.superpowers/

# Backend static (built SPA output)
backend/app/static/

# Real k8s secret manifests (copied from deploy/k8s/secret.example.yaml) --
# never commit filled-in credentials.
deploy/k8s/secret.yaml

# Env / OS
.env
.DS_Store
