"""Notification channel framework: the `NotificationChannel` ABC that both
the built-in email channel and third-party plugins implement, plus the
registry that discovers them (see `base.py` / `registry.py`).
"""
